/**
 * Exclusive-or gate:
 * if (a == b) out = 1, else out = 0
*/

CHIP Xor {
  IN a, b;
  OUT out;

  PARTS:
  Nand(a=a, b=b, out=w1);
  Or(a=a, b=b, out=w2);
  Nand(a=w1, b=w2, out=w3);
  Not(in=w3, out=out);
}
