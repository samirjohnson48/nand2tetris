/**
 * Demultiplexor gate:
 * if (sel==0) out[0]=in, out[1]=0, else out[0]=0, out[1]=in
*/

CHIP DMux {
  IN in, sel;
  OUT a, b;

  PARTS:
  Not(in=sel, out=nsel);
  And(a=in, b=nsel, out=a);
  And(a=in, b=sel, out=b);
}
